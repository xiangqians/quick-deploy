<?xml version="1.0" encoding="UTF-8"?>
<!--
 | debug      : 当此属性设置为 true 时，将打印出 logback 内部日志信息，实时查看 logback 运行状态。默认值为 false。
 | scan       : 当此属性设置为 true 时，配置文件如果发生改变，将会被重新加载，默认值为 true
 | scanPeriod : 设置监测配置文件是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。当 scan 为 true 时，此属性生效。默认的时间间隔为 1 分钟。
 |-->
<configuration debug="false" scan="false">

    <!--
     | property 用来定义变量值的标签，有两个属性：name 和 value。
     | 其中 name 的值是变量的名称，value 的值时变量定义的值。
     | 通过定义的值会被插入到 logger 上下文中。
     | 定义变量后，可以使 “${}” 来使用变量。
     |-->

    <!-- 日志文件路径 -->
    <property name="PATH" value="log"/>

    <!--
     | 日志格式
     | %d{yyyy-MM-dd HH:mm:ss.SSS} : 记录时间格式
     | %p                          : 日志级别
     | %t                          : 线程名
     | %logger                     : 全类名
     | %M                          : 方法名
     | %L                          : 行数
     | %m                          : 消息
     | %n                          : 换行符
     |-->
    <property name="PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %p [%t] %logger#%M:%L %m%n"/>

    <!-- 单个日志文件最大大小，超过则创建一个新的日志文件，日志文件的名字最后+1 -->
    <property name="MAX_FILE_SIZE" value="1GB"/>
    <!-- 日志文件保留天数 -->
    <property name="MAX_HISTORY" value="30"/>
    <!-- 所有的日志文件最大大小，超过则会删除旧的日志 -->
    <property name="TOTAL_SIZE_CAP" value="10GB"/>

    <!-- 控制台日志追加器 -->
    <appender name="CONSOLE_APPENDER" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${PATTERN}</pattern>
            <!-- 设置字符集 -->
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>

    <!-- debug 级别日志文件追加器 -->
    <appender name="DEBUG_FILE_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${PATH}/debug.log</file>
        <encoder>
            <pattern>${PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${PATH}/%d{yyyy-MM}/debug.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
        <!-- 只记录 debug 级别日志 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <!-- debug 级别日志 -->
            <level>DEBUG</level>
            <!-- 表示匹配该级别及以上 -->
            <onMatch>ACCEPT</onMatch>
            <!-- 表示不匹配该级别及以上 -->
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- info 级别日志文件追加器 -->
    <appender name="INFO_FILE_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${PATH}/info.log</file>
        <encoder>
            <pattern>${PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${PATH}/%d{yyyy-MM}/info.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
        <!-- 只记录 info 级别日志 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- warn 级别日志文件追加器 -->
    <appender name="WARN_FILE_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${PATH}/warn.log</file>
        <encoder>
            <pattern>${PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${PATH}/%d{yyyy-MM}/warn.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
        <!-- 只记录 warn 级别日志 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>WARN</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- error 级别日志文件追加器 -->
    <appender name="ERROR_FILE_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${PATH}/error.log</file>
        <encoder>
            <pattern>${PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${PATH}/%d{yyyy-MM}/error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
        <!-- 记录 error 级别及以上的日志 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <!-- 根日志记录器配置 -->
    <!-- 如果设置根日志记录器的日志级别为 INFO，意味着只有 INFO 级别及以上的日志事件才会被根日志记录器处理和输出，更低级别的日志事件，如 DEBUG 或 TRACE 将被忽略 -->
    <!-- 日志级别: TRACE < DEBUG < INFO < WARN < ERROR < FATAL -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE_APPENDER"/>
        <appender-ref ref="DEBUG_FILE_APPENDER"/>
        <appender-ref ref="INFO_FILE_APPENDER"/>
        <appender-ref ref="WARN_FILE_APPENDER"/>
        <appender-ref ref="ERROR_FILE_APPENDER"/>
    </root>

</configuration>